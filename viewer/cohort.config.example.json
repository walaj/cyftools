{
  "_comment": "Optional preferences for the cohort viewer. Every key is optional; load it alongside a cohort.json via ?config=... (or just drag it onto the page).",

  "group_colors": {
    "Responder": "#2a73d8",
    "NonResponder": "#c0392b"
  },

  "default_metric": "density",

  "default_compartment": "Tumor",

  "export": {
    "format": "png",
    "width": 1100,
    "height": 700,
    "scale": 2
  }
}
